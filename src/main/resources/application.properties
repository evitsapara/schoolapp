# keep as fallback
# java -jar schoolapp.jar --spring.profile.active-prod
spring.profiles.active=dev

# server.servlet.context-path=/schoolapp

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver